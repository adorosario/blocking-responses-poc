# Railway Environment Configuration
# Copy these variables to your Railway project environment settings

# Required - Add your OpenAI API key in Railway dashboard
OPENAI_API_KEY=

# Model Configuration
DEFAULT_MODEL=gpt-4o-mini
JUDGE_MODEL=gpt-4o-mini

# Logging Configuration
LOG_LEVEL=INFO

# Security Settings
DELAY_TOKENS=20
DELAY_MS=250
RISK_THRESHOLD=1.0
JUDGE_THRESHOLD=0.8
ENABLE_JUDGE=true
ENABLE_SAFE_REWRITE=true
ENABLE_AUDIT_LOGGING=true
HASH_SENSITIVE_DATA=true
PRESIDIO_CONFIDENCE_THRESHOLD=0.6

# Production Settings
PORT=$PORT
HOST=0.0.0.0

# CORS Configuration - Railway auto-generates these domains
CORS_ORIGINS=https://*.up.railway.app,https://*.railway.app